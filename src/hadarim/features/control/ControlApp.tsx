import { PageHeader } from "../../../components/primitives";

/** Placeholder — replaced by the control conversation screen (phase 3). */
export function ControlApp() {
  return (
    <main style={{ padding: 24 }}>
      <PageHeader title="בקרה — הדרים" subtitle="מסך הבקרה בבנייה" />
    </main>
  );
}
