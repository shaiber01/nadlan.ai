import { PageHeader } from "../../../components/primitives";

/** Placeholder — replaced by the simulated ERP screens (phase 2). */
export function ErpApp() {
  return (
    <main style={{ padding: 24 }}>
      <PageHeader title="זיו — סביבת הדגמה" subtitle="מסכי מערכת המידע בבנייה" />
    </main>
  );
}
