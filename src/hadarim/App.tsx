import { PageHeader } from "../components/primitives";

/** Placeholder until the v2 screens land (phases 2–6 of docs/hadarim-v2-plan.md). */
export function HadarimApp() {
  return (
    <main className="h2-shell" style={{ padding: 24 }}>
      <PageHeader title="בקרה תקציבית — הדרים" subtitle="סביבת הדגמה · בבנייה" />
    </main>
  );
}
